{
  "name": "construct3-kit",
  "version": "1.0.0",
  "description": "Tools for Construct3 projects,",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "bash ./setup-justjs.sh && npm run build && cp -r ./public ./dist/public && electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "construct3",
    "image",
    "points"
  ],
  "author": "JeyDotC",
  "license": "MIT",
  "dependencies": {
    "@jeydotc/justjs": "^1.0.1",
    "image-pixels": "^2.2.2"
  },
  "devDependencies": {
    "electron": "^17.0.1",
    "typescript": "^4.6.2"
  }
}
